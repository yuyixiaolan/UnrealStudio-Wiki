# 虚幻引擎开发规范

这里是虚幻工作室的 C++ 和蓝图命名与开发规范。

## 一、 命名规范
* **类名**：使用大写前缀（如 `AMyActor`、`UStaticMeshComponent`）。
* **变量名**：使用 PascalCase（驼峰命名法，首字母大写），例如 `HealthPoints`。

## 二、 蓝图规范
* 尽量避免连线交叉。
* 复杂的逻辑应当封装进函数或宏。